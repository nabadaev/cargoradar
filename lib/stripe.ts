// TODO: Add STRIPE_SECRET_KEY to .env.local and install stripe package:
//   PATH="/Users/Nikita/.local/node/bin:$PATH" npm install stripe
//
// Then replace this stub with:
//   import Stripe from 'stripe'
//   export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, { apiVersion: '2024-04-10' })

export const stripe = null // stub — Stripe not yet configured
