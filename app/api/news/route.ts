import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'News API — Phase 4' })
}

export async function POST() {
  return NextResponse.json({ message: 'News API — Phase 4' })
}
