'use client'

// Phase 3: Mapbox GL JS map with hot zones and trade lanes
export default function MapView() {
  return (
    <div style={{ width: '100%', height: '100%', background: 'var(--off)' }}>
      {/* Mapbox map mounts here — Phase 3 */}
    </div>
  )
}
